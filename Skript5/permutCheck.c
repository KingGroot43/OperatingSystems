#include <stdio.h>

int isPermutation(char *a, char *b) {
}

int main(void) {
    int i = 1;
    int j = i + 2;
    printf("%i", j);

    //isPermutation(strA,strB);
    //isPermutation("hello", "lehho");
}